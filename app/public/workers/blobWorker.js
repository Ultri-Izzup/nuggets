/**
 * Worker script dedicated to handling blobs from the browser,
 * writing the blob content to OPFS in the given directory, and then
 * creating a record in the Dexie/IndexedDB assets linking to the nugget.
 */
